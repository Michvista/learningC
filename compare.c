#include <stdio.h>

int main(void)
{
    int x = 30;
    int y= 20;

    if (x < y)
    {
        printf("x is less");
    }    
   else if (x < y)
   {
    printf("x is greater");
   }
   else{
    printf("equal");
   }

}